# 검증 — 틀

> 이 작업의 **검증**. **구현 세션과 다른 세션이 채운다** — 만든 사람이 자기 작업을 판정하지 않는다.
> [중간결과물.md](중간결과물.md)의 값을 **기준 칸으로 옮기고**, 독립 명령으로 재산출해 대조한다.
> **기준은 실측 전에 기입한다** — 결과를 보고 기준을 맞추면 검증이 아니라 기록이다.
> 집계(통과 n/실패 n/미검증 n)가 [최종결과물.md](최종결과물.md) 결론의 M/N **유일한 원천**.
> 6열은 Pusher `docs/template/검증.md`(260806 확정) 고정 열을 그대로 상속.
>
> ⚠️ **현재 상태 = 기준만 사전등록됐다**(260809 거베라). **실측·판정 칸은 전부 비어 있다.**
> 실측 세션이 채우고 제목에 자기 세션명을 덧붙인다. 기준을 고친다면 **고친 이유를 '차이·특이'에 남긴다** —
> 실측을 보고 조용히 기준을 옮기면 위 규칙이 무너진다.

---

# ros2_control 하드웨어 인터페이스 구성 — 검증 (기준 260809 거베라 / 실측 미기입)

**집계**: 통과 0 / 실패 0 / **미검증 15**
**환경**: 기준 산정 기준선 = 09pc · 컨테이너 `moveit2_dev:jazzy`(ros2_control **4.45.2**) · `markch/hcr5_ros2@f4d9837` · 실기 HCR-5 `192.168.0.20`
— *실측 세션이 자기 환경으로 이 줄을 덮어쓴다. 환경이 다르면 다른 검증이다.*

## 검증표 (행 = 독립 판정 가능한 확인 1건)

| ID | 항목 | 방법 | 기준 | 실측 | 판정 |
|---|---|---|---|---|---|
| V-1 | 빌드 | `flock /tmp/atlier_colcon.lock colcon build --packages-select hcr_bridge` | 빌드 성공 · `-Wall -Wextra -Wpedantic` 경고 **0건** | | 미검증 |
| V-2 | 플러그인 발견 | `ros2 control list_hardware_components` | `hcr_robot_system` **1개** · state `active` · 출력에 `read/write rate: 30 Hz` · `is_async: True` | | 미검증 |
| V-3 | 인터페이스 노출 수 | `ros2 control list_hardware_interfaces` | command position **6** / state position **6** / state velocity **6**. 전부 `available`, command 는 `claimed` | | 미검증 |
| V-4 | 컨트롤러 활성화 | `ros2 control list_controllers` | `hcr_arm_controller`·`joint_state_broadcaster` **둘 다 `active`** (2개) | | 미검증 |
| V-5 | 상태 정합 (규약 변환) ★ | 실기 관절값(펜던트) ↔ `/joint_states`. 정답지 = 중간결과물 §대조 정답지 4열 | 6축 **전부** 정답지와 편차 **≤0.05°**(8.7e-4 rad). **홈 자세 + 다른 자세 ≥1점** — 1점만이면 판정 '미검증' | | 미검증 |
| V-6 | `read()` 실효 주기 | `/joint_states` **값이 바뀌는 빈도**를 센다. ⚠️ `ros2 topic hz` 는 못 쓴다 — 브로드캐스터가 `update_rate`(100Hz)로 내보내 `rw_rate` 를 안 비춘다(S2 확인) | 값 갱신 **30±3Hz** (버스 실측 29.21·29.26Hz) | | 미검증 |
| V-7 | `write()` 도달 오차 | 실기: 정지 상태에서 소각도 목표 **1개**(base +5°, `set/velocity` reducedVelocity **0.5**) → `event/motion {"event":"moveHere"}` 수신 후 관절값 | 전축 \|목표−도달\| **≤0.01°** (movej 실측 0.0000° · 데드밴드 0.01°, hcr_comm README §5.2) | | 미검증 |
| V-8 | 안전 게이트 (잠금 시 무동작) | 실기: `allow_motion=false` 로 JTC 궤적 실행 + 버스 구독 동시 | `move/joint/here` 발행 **0건** · 관절값 p-p **≤0.01°**. **1건이라도 발행되면 실패** | | 미검증 |
| V-9 | 통신 두절 거동 | 랜선 분리 후 `read()` 반환값·컨트롤러 상태 관찰 | **1.0±0.2s** 안에 `return_type::DEACTIVATE` · 컨트롤러 비활성 전이 · 낡은 값 발행 중단 | | 미검증 |
| V-10 | mock 기본 경로 회귀 | 인자 **없이** `ros2 launch hcr_moveit_config demo.launch.py` | 컴포넌트가 `read/write rate: 100 Hz`·`is_async: False` · `/joint_states` **100±5Hz** · 기동 직후 자세가 SRDF `hcr_home` 대비 오차 **≤1e-6 rad** | | 미검증 |
| V-11 | 실기 없이 실기 모드 로드 | 무응답 호스트(`192.168.0.99:1883`)·접속 거부(`127.0.0.1:18830`)·브로커만 3경로 | `on_configure` 가 **≤3.1s** 안에 ERROR, **경로마다 다른 메시지**. OS 타임아웃(수십 초)에 잡히면 실패 | | 미검증 |
| V-12 | CM abort 완화책 | `hardware_components_initial_state.{unconfigured,shutdown_on_initial_state_failure}` 설정 후 실기 없이 기동. ⚠️ **낡은 `robot_state_publisher` 를 먼저 완전히 죽인다**(전송지속 QoS 오염, S1 실패 원인) | 프로세스가 **SIGABRT 하지 않는다**. 여전히 abort 하면 실패 = 완화 불가 | | 미검증 |
| V-13 | 이동 중 차분 velocity 품질 | C 궤적 실행 중 `/joint_states.velocity` ↔ position 수치미분 대조 | 이동 구간 부호 일치 **100%** · 크기 오차 **≤10%**. (정지 구간은 S3 가 0.0000 rad/s 확인 — 재확인 대상) | | 미검증 |
| V-14 | 재발행 선점 | 실기: 이동 중 새 `move/joint/here` 발행 → 선행 이동 거동 관찰 | 새 목표가 선행 이동을 **선점한다**. 선행을 끝까지 가거나 명령이 거부되면 **실패** — 137ms 재발행 설계(§5)가 성립 안 한다 | | 미검증 |
| V-15 | JTC 궤적 관통 | `/move_action` 으로 짧고 느린 관절 궤적 **1개** Plan&Execute | `error_code = 1`(SUCCESS) · 궤적 허용오차 abort **없음**. abort 시 실패 — 그 경우 §2 '보간 소유자' 계약을 점대점으로 좁히는 판정 근거가 된다 | | 미검증 |

**행 규칙**
- 복수 확인 **병합 금지** — 병합되면 1건의 실패가 통과에 동봉된다
- **행 삭제 금지** — 실행 못 한 행은 실측 칸에 사유 한 단어(실기 필요/접근 불가/소진) + 판정 '미검증'
- 실행 후 추가한 행은 ID 에 `+` 표시
- 기각 조건이 도출 안 되는 기준("0건 이상")은 **등록 무효**
- 판정은 3값, 실측이 기준 밖이면 **자동 실패** (재량 통과 금지)

**행 유래** — V-1~V-9 는 틀의 원 9행(항목명 유지). V-10~V-15 는 중간결과물의 표값 중 원 9행이 안 덮는 것을
사전등록 때 추가했다(**실행 전 추가라 `+` 표시 없음**): §4 mock 회귀·§3 A3·§3 CM abort·§1 velocity 산출·§5 재발행·§2 보간 소유자.

**실기 필요 여부** — 실기 **불필요**: V-1·V-10·V-11·V-12. 실기 **읽기**(서보 OFF): V-2·V-3·V-4·V-5·V-6·V-9.
실기 **쓰기**(서보 ON·Mark 입회): V-7·V-8·V-13·V-14·V-15.

## 차이·특이 (≤3줄)
- **기준을 실측보다 먼저 등록했다**(260809 거베라, Mark 승인) — 구현자(수련)와 다른 세션이고 B·C 실기 대면 **전**이다. 실측 세션은 이 기준을 받아 쓴다.
- **V-6 의 방법이 틀에서 바뀌었다** — 틀은 `ros2 topic hz /joint_states` 였으나 그 토픽은 `rw_rate` 를 안 비춘다(S2 실측). 값 갱신 빈도로 교체했다.
- `motion/joint/speed` 단위·부호(T19)는 **검증 항목이 아니다** — 계약(position 차분 유지)의 **재검토 트리거**이고, 계약 자체의 검증은 V-13 이 한다.

## 남은 것 (≤3줄)
- **15행 전부 미검증이다.** 실측은 B(읽기)·C(쓰기) 실기 대면 뒤에야 채워진다 — 잔여 지도 = 루트 `briefs/잔여작업.md`
- **V-5 는 정답지가 홈 자세 1점뿐이라 지금 그대로 재면 '미검증'이다** — 다른 자세 ≥1점 확보가 선행이다
- V-7·V-8·V-13·V-14·V-15 는 **서보 ON 창 1회**에 몰아야 한다 (축온 비대칭 3배, 업무목록 T20)

## 판정 (Mark)
판정: (미기입 = 미확정 — 기입 전 업무목록 '닫힘' 이동 금지, 에이전트 대필 금지)
