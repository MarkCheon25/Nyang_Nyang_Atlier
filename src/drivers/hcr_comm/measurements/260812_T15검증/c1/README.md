# 검증 재현 원자료 — 검C1 (2026-08-12, 04pc · 260812-스위치허브)

`검증.md` 15행 중 **V-8**(안전 게이트) 1행을 이 PC 에서 다시 실행한 원자료다.
**서보 ON 창 17.45초** · `allow_motion=false` · JTC `joint_1` +5° · **로봇 무동작**.

> **왜 상위 디렉터리에 안 담고 `c1/` 을 팠나** — 상위 `260812_T15검증/` 은 검B 산물이고 그 README 가
> *"읽기 전용으로 다룬다"* 를 선언했다. 계획서 §4-1 은 검B·C1·C2 가 **같은 날짜 디렉터리**를 쓰라고 했으므로
> 날짜 디렉터리는 그대로 두고 **세션 몫을 하위로 갈랐다.** 검B 파일은 하나도 건드리지 않았다.

- 실행 코드 = `markch/hcr5_ros2` **`eb7bf18`** · 작업트리 clean
- 실기 HCR-5 `192.168.0.20` · 04pc `enp3s0` = `192.168.0.100/24`
- **분석기는 상위 `verify/c1_*` 에 있다** — 다음 세션이 다시 짜지 않는다(계획서 §4-2)
- 트레이스 2종은 **`.gz` 로 보관한다**(260811 관례). 4.9MB → **352KB**.
  `c1_an.py` 가 `.gz` 를 그대로 읽고 **압축 전후 산출이 완전 일치**함을 확인했다

## 판정 요약 — V-8 기준 전 항목 충족

| 항목 | 기준 | **검C1** | 08-11 대조(강한 형태) | 판정 |
|---|---|---|---|---|
| `move/joint/here` | **0건** | **0건** (`move/*` 전체 0건) | 0건 | **충족** |
| 감청 유효성 | 0건이 위음성 아님을 먼저 세울 것 | **9097건·20토픽** · `set/operation` 2 + ack 2 포착 | 10237건·22토픽 | **충족** |
| **궤적 구간** 관절 p-p | **≤0.01°** | **0.000272°** | ≤0.000163° | **충족** |
| 추종오차(판정 근거) | — | `joint_1` **정확히 5.0000°** | 5.0000° | 일치 |

⚠️ p-p 기준은 **궤적 구간** 문면(계획서 §3, Rokey6 08-11 판정)으로 쟀다. `검증.md` 의 낡은 문면(창 전체)이 아니다 —
**같은 창을 창 전체로 재면 0.0515° 로 기준 밖**이 되어 정상 거동이 실패로 뒤집힌다(아래).

## 파일 지도

| 파일 | 무엇 | 근거가 되는 곳 |
|---|---|---|
| `bus/c1_bus.jsonl.gz` | 버스 전량 타임스탬프 (40s · **9097건 · 20토픽** · 원본 2.7MB) | **V-8** `move/joint/here` 0건 · 감청 유효성 |
| `ros/c1_ros.json.gz` | `/joint_states` 4007표본 + `controller_state` 4000표본 (원본 2.3MB) | **V-8** 추종오차 5.0000° · 구간별 p-p |
| `logs/c1_marks.txt` | 창 경계 epoch 마크 7종 (`servo_on_post`·`jtc_send`·`jtc_done`·`servo_off_pre` …) | **구간 자르기의 원본** |
| `logs/c1_temp.jsonl` | 축온 393표본 (10Hz · 6축) | 축온 55→56°C · 감시견 미발동 |
| `logs/c1_tempwatch.log` | 감시견 실시간 출력 | 〃 |
| `logs/C1_launch.log` | 창 전 구간 런치 로그 (기동→종료) | `allow_motion=false` 확인(33–34행) · 결함 ③ · V-6 손실통계 |
| `logs/c1_jtc.log` | JTC 액션 결과 | `error_code=0` SUCCEEDED — **5° 오차를 두고** (발견 ① 재확인) |
| `logs/c1_analysis.md` | `verify/c1_an.py` 출력 전문 | 위 판정표의 산출 근거 |
| `logs/c1_bus.log` · `c1_ros.log` | 계측기 자체 로그 | 수집 건수 확인 |

## 재현 방법

```bash
nmcli connection up hcr5 && docker start markch_moveit2_dev
# 스택 기동 (allow_motion=false)
docker exec markch_moveit2_dev bash -lc 'cd ~/ws_moveit2 && source install/setup.bash && \
  ros2 launch hcr_moveit_config demo.launch.py \
  hardware_plugin:=hcr_bridge/HcrSystemInterface rw_rate:=30 is_async:=true allow_motion:=false'
docker cp ../../../tools/ros_trace.py markch_moveit2_dev:/tmp/ros_trace.py   # 마운트 밖이라 필요
bash ../verify/c1_run.sh        # ⚠️ 서보 ON — Rokey6 입회·비상정지 대기에서만
python3 ../verify/c1_an.py
```

## ★ 이 원자료가 보여 주는 것 — 구간을 안 자르면 결론이 뒤집힌다

| 구간 | 길이 | 표본 | 최대 p-p | 어느 축 | 기준 대비 |
|---|---|---|---|---|---|
| 창 전체 | 40.06s | 4007 | **0.051505°** | joint_4(wrist1) | ❌ **밖** |
| 서보 ON 구간 | 17.45s | 1745 | 0.006962° | joint_3 | 안 |
| **궤적 구간** | 7.44s | 744 | **0.000272°** | joint_4 | ✅ **안** |

관절값 변화 **43회** 중 궤적 구간 안은 **9회**뿐이고 나머지는 서보 ON(t=3.57s)·OFF(t=21.02s) 전이 부근에 몰렸다.
**브레이크 해제·체결의 중력 정착**이지 명령 누출이 아니다 — 명령한 축 `joint_1` 은 **창 전체로도 정확히 `0.000000°`** 다.

## 착수 게이트 — 축온이 기준을 넘긴 채 진행했다

착수 시 **55°C**(기준 ≤50°C). 검B 종료값과 **실질 +0** 이라 기다려도 안 내려간다(실온 평형, 2회 독립 측정).
**Rokey6 판정 = 진행, 창 ≤20초 + 58°C 즉시중단**(2026-08-12). 근거는 검B 의 *"서보 OFF 라 무해"* 가 아니라
**08-11 이 같은 V-8 을 17.3초 돌려 +1°C** 를 실측한 선례다. 결과 **55 → 56°C(+1)** 로 예측이 맞았고 감시견은 발동하지 않았다.
차단은 `verify/c1_tempwatch.py` 가 10Hz 로 걸었다.

⚠️ **이 판정은 검C2 에 적용되지 않는다** — C2 는 창이 길고 **로봇이 실제로 움직여** 발열이 홀딩 부하보다 크다.
